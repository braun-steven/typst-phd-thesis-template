#import "../definitions.typ": *
#import "../constants.typ": *

#table(columns: (1fr, 4fr), stroke: none, fill: none,
    // --- Mythic Modeling ---
    table.cell(colspan: 2)[*Mythic Modeling*],
    [$M$], [Manifold representing a universe],
    [$g(M)$], [Pantheon count inferred for manifold $M$],
    [$chi(M)$], [Euler characteristic as a divine predictor],
    [$psi(t)$], [Faith flux over cosmic time $t$],
    [$a(x)$], [Baseline aura intensity measured at location $x$],
    [$h(Xb)$], [Halo annotations across regions $Xb$],
    [$b(y)$], [Probability mass for observing a blessing $y$],
    [$c(y | r)$], [Conditional curse likelihood after ritual $r$],
    [$Omega$], [Collection of symbolic observations],
    [$s(Omega)$], [Encoding function mapping omens to tokens],
    [$sigma_t$], [Noise scale for illuminating hidden relics],
    [], [],

    // --- Fictional Inference ---
    table.cell(colspan: 2)[*Fictional Inference*],
    [$pi_0$], [Oracle prior used for divination],
    [$S$], [Posterior sampler consulting the oracle],
    [$v$], [Latent variable masking inconvenient truths],
    [$r(x)$], [Posterior revelation for event $x$],
    [$m(xb)$], [Likelihood of a myth given storyline $xb$],
    [$t(xb)$], [Counter-likelihood under a skeptic baseline],
    [$eta$], [Learning rate for daybreak updates],
    [$lambda$], [Decay factor for forgetting prophecies],
    [$p_+$], [Predictive distribution for the next omen],
    [], [],

    // --- Ritual Circuits ---
    table.cell(colspan: 2)[*Ritual Circuits*],
    [$C$], [Ritual circuit describing ceremonial flow],
    [$S_g$], [Sigil graph controlling gateways],
    [$sigma_j$], [Weights assigned to chanting paths],
    [$rho_i$], [Input rune index],
    [$kappa_j$], [Intermediate chant aggregator],
    [$phi_k$], [Fusion node combining offerings],
    [$X_f$], [Set of omens governed by fusion $f$],
    [$A(f)$], [Alignment operator ensuring harmonious inputs],
    [$w$], [Dropout mask preventing malevolent overfitting],
    [$zeta$], [Normalization constant keeping rituals stable],
)
